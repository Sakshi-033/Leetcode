class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False

        a1 = list(s)
        a2 = list(t)

        for i in a1:
            if i in a2:
                a2.remove(i)
            else:
                return False

        return True
